<div class="btn-return-to-home fade-in-effects-initial"
     data-animation-fade-in-target="fadeIn"
     data-animation="fade-in-up-effects">
    <button class="btn" type="button" aria-label="{{ 'navigation.return_home_aria' | trans }}"
            data-controller="redirect"
            data-action="click->redirect#redirectToUrl"
            data-redirect-url-value="{{ path('app_main_index') }}"
    >
        {{ 'navigation.return_home' | trans }}
    </button>
</div>