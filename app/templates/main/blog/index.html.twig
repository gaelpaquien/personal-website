{% extends 'base.html.twig' %}

{% block title %}{{ 'blog' | trans }} - {{ 'site_title' | trans }}{% endblock %}

{% block content %}
    <section id="blog" class="blog__container">
        <div class="fade-in-effects-initial"
             data-animation-fade-in-target="fadeIn"
             data-animation="fade-in-up-effects"
        >
            <div class="section-title link-effects blog__container-title">
                {% embed 'assets/svg/star.html.twig' %}
                    {% block attributes %}
                        aria-labelledby="blog__container-title-h2"
                    {% endblock %}
                {% endembed %}
                <h1 id="blog__container-title-h2">{{ 'blog' | trans }}</h1>
            </div>
        </div>

        <div class="blog__container-posts"
             data-controller="blog-card"
        >
            {% for post in posts %}
                {% include "components/blog/card.html.twig" with {
                    'title': post.title,
                    'content': post.content,
                    'image': post.image,
                    'tags': post.tags,
                    'updated_at': post.updated_at,
                    'slug': post.slug
                } %}
            {% endfor %}
        </div>
    </section>
{% endblock %}